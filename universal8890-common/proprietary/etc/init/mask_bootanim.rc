# fixes phh gsis, which starts bootanim in charger mode for some reason
on charger
    exec_start mask_bootanim

service mask_bootanim /vendor/bin/mask_bootanim
    disabled
    oneshot
    user root
    group root
    seclabel u:r:su:s0

